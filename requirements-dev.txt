# 开发/CI 依赖（不进离线交付包，产品运行不需要；见 scripts/offline/make_bundle.sh）
# 安装： .venv/bin/python -m pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0          # 覆盖率门槛（scripts/test_gate.sh）
requests>=2.31           # API 压测脚本 scripts/stress_test_full.py
playwright>=1.44         # 前端自动化（tests/e2e_frontend_smoke.py），需 playwright install chromium
