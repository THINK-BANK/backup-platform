# 目标端「无 Agent / 零残留」真实取证报告

> 日期：2026-09-19 14:55:20　目标：root@127.0.0.1:2222（真实 SSH）　数据库：MySQL 127.0.0.1:3306/forensic_demo
> 口径：取证命令真实执行于目标端（ps / find / crontab / ls），**无仿真**；并投放了负向对照证明该取证不是「只能报 PASS」。

## 一、结论

- 通过 **23** 项 / 失败 **0** 项
- 备份前取证结论 **PASS**；执行 MySQL 物理备份后再次取证结论 **PASS**

## 二、取证分节真实输出

```json
{
  "processes": [],
  "temp_files": [],
  "crontab": [],
  "systemd": [],
  "packages": []
}
```

## 三、负向对照（关键：证明判定有效）

| 步骤 | 目标端真实状态 | 取证结论 |
|---|---|---|
| 投放 /tmp/bk_agentless_canary | 文件存在 | **WARN**（已被指认到文件） |
| 清除该文件 | 已删除 | **PASS** |

## 四、执行期临时二进制（A2）的进出记录

备份执行期在目标端 /tmp 采样到的平台侧临时文件（真实存在）：

- `bk_pushed_xb_1789800914`
- `bk_pushed_xb_libs`

备份结束后再次取证，以上路径**均已不存在**（见第二节每个分节均为空列表）。

## 五、通道计划（本次任务）

```json
{
  "task_name": "agentless-forensics-physical",
  "db_type": "mariadb",
  "backup_mode": "physical",
  "target": "root@127.0.0.1:2222",
  "max_level": "A2",
  "temp_binary": true,
  "requires_target_config": []
}
```

## 六、明细与边界（如实标注）

1. ✅ F1.1 平台命名残留 → 判 WARN/FAIL
2. ✅ F1.2 平台常驻进程 → 判 FAIL（红线）
3. ✅ F1.3 客户自有软件 → 不替客户背锅（PASS）
4. ✅ F1.4 计划任务含 aidbm → 判 FAIL
5. ✅ F1.5 无分节输出 → UNKNOWN（不可采信）
6. ✅ F1.6 MySQL 物理+实时任务 → 推算 A3（含 A2 临时二进制）
7. ✅ F2.0 纳管主机可用
8. ✅ F2.1 取证取回有效分节（非 UNKNOWN）
9. ✅ F2.2 备份前结论为 PASS
10. ✅ F3.1 目标端投放平台风格残留成功
11. ✅ F3.2 取证发现该残留（非 PASS）
12. ✅ F3.3 并可指认到具体文件
13. ✅ F3.4 清理后回到 PASS
14. ✅ F3.5 清理命令确实是删除（不是伪装）
15. ✅ F4.1 MySQL 物理备份真实成功
16. ✅ F4.2 执行期确实把临时二进制推送到了目标端（不是嘴上说 A2）
17. ✅ F4.3 产物真实落盘
18. ✅ F5.1 备份后结论与备份前一致（PASS）
19. ✅ F5.2 备份后无任何 UID/命名可归因于平台的进程
20. ✅ F5.3 备份后无 crontab / systemd / 安装包残留
21. ✅ F6.1 该任务的计划识别出 SSH 通道
22. ✅ F6.2 计划与真实一致：确为 A2 临时二进制路径
23. ✅ F6.3 面板接口可推导全量任务计划

### 未验证边界

| 项目 | 结论 |
|---|---|
| 目标机文件系统全量比对 | 未做（取证是抽样式只读命令，不做全盘枚举，避免干扰目标端） |
| Windows 目标端 | 未验证（ps/crontab 为 Linux 命令，Windows 分支尚未实现） |
| 第三方云主机 / 137 达梦金仓 | 未验证（环境离线） |
| 临时二进制推送场景 | 已真实采样到进出 |

> 备注：为让本机能作为 SSH 目标，脚本所在机器已对本机 root 开启了公钥登录（`~/.ssh/authorized_keys`）；还原方式：删除其中本轮追加的那一行（备份文件 `~/.ssh/authorized_keys.bak_oss`）。
