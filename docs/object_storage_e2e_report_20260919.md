# 对象存储（MinIO / OSS / COS / S3 兼容）备份恢复 — 真实端到端验证报告

> 日期：2026-09-19 20:21:32　环境：本机 Docker MinIO（127.0.0.1:9000，凭据 minioadmin/••••）
> 口径：全部为真实 HTTP 请求与真实数据比对，**无仿真、无占位**。

## 一、结论

- 通过 **32** 项 / 失败 **0** 项
- 传输层为平台自研 S3 协议客户端（零第三方依赖），SigV4 签名已在真实 MinIO 上通过鉴权；
- 备份/恢复/增量/版本/细粒度/校验链路均与真实对象数据逐字节核验。

## 二、验证明细

1. ✅ S0 连通性（自研 SigV4 客户端真实通过 MinIO 鉴权）
2. ✅ S1.1 全量备份成功
3. ✅ S1.2 索引对象数与桶内一致
4. ✅ S1.3 中文/嵌套/空对象均入列
5. ✅ S1.4 备份容量与源一致
6. ✅ S1.5 包内对象数据与源数据逐字节一致
7. ✅ S1.6 深度校验通过
8. ✅ S2.1 增量备份成功
9. ✅ S2.2 只传输变化对象（改1+增1=2 个，未包含其余 4 个）
10. ✅ S2.3 记录删除事件
11. ✅ S2.4 增量包带 parent 指针指向全量
12. ✅ S3.1 恢复执行成功
13. ✅ S3.2 目标桶与备份时状态完全一致（含中文/嵌套/空对象）
14. ✅ S4.1 链路回放成功（写入8个）
15. ✅ S4.2 回放后与变更后状态一致（含被删除对象不复存在）
16. ✅ S5.1 前缀重映射恢复成功
17. ✅ S5.2 对象落在指定前缀下且数量正确
18. ✅ S5.3 overwrite=if_newer 对已存在且一致的对象跳过
19. ✅ S5.4 overwrite=never 全部跳过
20. ✅ S6.1 版本模式备份成功
21. ✅ S6.2 捕获到该 key 的全部历史版本（3 个）
22. ✅ S6.3 版本恢复后最新版本内容正确（v3）
23. ✅ S6.4 目标桶还原出版本历史（3 个版本）
24. ✅ S7.1 仅指定对象被恢复到目标桶
25. ✅ S7.2 未选择的对象不在目标桶
26. ✅ S8.1 正常包校验通过
27. ✅ S8.2 损坏包如实判失败
28. ✅ S8.3 产物缺失如实判失败
29. ✅ S9.1 桶不存在 → 备份失败并给出原因
30. ✅ S9.2 密钥错误 → 备份失败并给出原因
31. ✅ S9.3 缺少必填配置 → 失败并指明缺什么
32. ✅ S9.4 恢复产物不存在 → 失败

## 三、已验证与未验证边界（如实标注）

| 能力 | 实测结论 |
|---|---|
| MinIO / S3 兼容 全量备份 | ✅ 真实验证（含中文、深层嵌套、空对象、二进制） |
| 永远增量（ETag/LastModified/Size 差分） | ✅ 只传变化对象，删除事件入索引 |
| 增量链恢复（parent 指针） | ✅ full → inc 回放后与变更态逐字节一致 |
| 前缀重映射 / 覆盖策略 | ✅ always / if_newer / never 行为符合预期 |
| 版本捕获（Versioning） | ✅ 多版本对象捕获并还原出版本历史 |
| 对象级细粒度恢复 | ✅ 仅指定 key 被写入目标桶 |
| 深度校验（manifest + SHA256 抽样） | ✅ 正常包通过、损坏/缺失包如实失败 |
| 阿里云 OSS（V4 签名） | ⚠️ 按公开文档实现，**本机无 OSS 资源未做真实验证** |
| 腾讯云 COS（S3 兼容模式） | ⚠️ 官方提供 S3 兼容 API，属同一 SigV4 通道，但**本机无 COS 资源未做真实验证** |
| 华为云 OBS | ⚠️ 同上，未提供兼容接口实测 |

> 单 PUT 上限取决于服务端（AWS S3 5GB）；分片上传（Multipart）尚未实现，超限对象会拿到服务端明文错误并如实失败，不会静默截断。
